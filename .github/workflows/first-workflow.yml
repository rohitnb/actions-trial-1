name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Print Something
        run: echo "Hello Rohit, this is from Actions!"
      - name: Something else
        run: |
          node -v
          npm -v